import React, { Component } from 'react';
import { View, Text } from 'react-native';

class MyTrip extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> MyTrip Component </Text>
      </View>
    );
  }
}

export default MyTrip;
